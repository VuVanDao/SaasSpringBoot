spring.application.name=demo

server.port=5000
server.servlet.context-path=/api

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

spring.datasource.url=jdbc:mysql://localhost:3306/personalProject
spring.datasource.username=root
spring.datasource.password=dao11082004

app.allowed-port = 3000
app.allowed-origins = http://localhost:${app.allowed-port}

app.header-tenant = X-tenant-Id

# Persist Authorize headers in Swagger UI across page refreshes
springdoc.swagger-ui.persistAuthorization=true
